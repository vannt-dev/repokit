[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "fixture-python"
version = "0.0.0"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["fixture"]
